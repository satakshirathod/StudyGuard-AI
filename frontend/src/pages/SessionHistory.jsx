import Placeholder from './Placeholder.jsx';

export default function SessionHistory() {
  return (
    <Placeholder
      title="Session History"
      phase="Coming in Phase 10–12"
      detail="List of past study sessions with duration, average focus score, drowsiness and phone events once the SQLite database and session service are live."
    />
  );
}
import Placeholder from './Placeholder.jsx';

export default function Students() {
  return (
    <Placeholder
      title="Students"
      phase="Coming in Phase 10–11"
      detail="Create and manage student profiles (name, email) via the /api/students endpoints, once the database and API layer are built."
    />
  );
}